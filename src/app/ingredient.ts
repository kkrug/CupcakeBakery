export interface Ingredient {
    name: string;             // Name of the ingredient (e.g., "Salt", "Butter")
    type: string;             // Type or category of the ingredient (e.g., "Seasoning", "Dairy")
    quantityCupcake: string;  // Quantity or measurement for cupcake (e.g., "1 teaspoon", "1 cup" - "NA" if not needed)
    quantityFrosting: string; // Quantity or measurement for frosting (e.g., "1 teaspoon", "1 cup" - "NA" if not needed)
  }
  
